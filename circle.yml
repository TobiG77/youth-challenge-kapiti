version: 2
jobs:
  build:
    docker:
     - image: tobig77/docker-deploy-hugo-to-s3:latest
       environment:
         ENV: CI

    working_directory: ~/

    steps:
      - checkout
      - run: bash ci/publish.sh
